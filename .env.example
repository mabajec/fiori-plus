DATABASE_URL=postgresql+psycopg://fiori:fiori@localhost:5432/fiori
INPUTS_DIR=./inputs
# Override in production. Generate with: python -c "import secrets; print(secrets.token_urlsafe(64))"
SESSION_SECRET=dev-secret-do-not-use-in-production
SESSION_HTTPS_ONLY=false
